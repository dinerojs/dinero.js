{
  "name": "@dinero.js/monorepo",
  "version": "3.0.0-alpha.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run res:build --workspaces",
    "clean": "npm run res:clean --workspaces",
    "dev": "npm run res:dev --workspaces",
    "format": "npm run res:format --workspaces",
    "test:size": "bundlesize",
    "format:check": "npm run res:format:check --workspaces",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "rescript"
  ],
  "author": "",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "rescript": "^12.1.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.0.0",
    "bundlesize": "^0.18.2",
    "vitest": "^1.0.0"
  }
}
