{
  "name": "rescript-dinero",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "res:build": "npm run res:build --workspaces",
    "res:clean": "npm run res:clean --workspaces", 
    "res:dev": "npm run res:dev --workspaces",
    "pretest": "npm run res:build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "rescript"
  ],
  "author": "",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "rescript": "^12.1.0"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "@vitest/coverage-v8": "^1.0.0"
  }
}
