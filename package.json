{
  "name": "@dinero.js/monorepo",
  "version": "3.0.0-alpha.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "yarn workspaces foreach --all run res:build",
    "clean": "yarn workspaces foreach --all run res:clean",
    "dev": "yarn workspaces foreach --all run res:dev",
    "format": "yarn workspaces foreach --all run res:format",
    "test:size": "bundlesize",
    "format:check": "yarn workspaces foreach --all run res:format:check",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "rescript"
  ],
  "author": "",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "rescript": "^12.1.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.0.0",
    "bundlesize": "^0.18.2",
    "vitest": "^1.0.0"
  },
  "packageManager": "yarn@4.12.0"
}
