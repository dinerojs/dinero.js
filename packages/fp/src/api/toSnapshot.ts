import { toSnapshot } from '@dinero.js/core';

/**
 * Get a snapshot of a functional Dinero object.
 *
 * @param dineroObject The functional Dinero object to transform.
 *
 * @returns A snapshot of the object.
 */
const functionalToSnapshot = toSnapshot;

export default functionalToSnapshot;
